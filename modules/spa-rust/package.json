{
  "name": "@module/spa-rust",
  "description": "",
  "version": "0.0.1",
  "scripts": {
    "test": "cargo test",
    "lint": "cargo clippy --all-targets --quiet",
    "fmt": "cargo fmt",
    "dev": "dx serve --port 3032",
    "build": "dx build --release",
    "preview": "dx serve --release --port 3032"
  }
}
