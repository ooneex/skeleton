[package]
name = "design-icons"
version = "0.1.0"
edition = "2024"
description = "Icon components of the design module, generated from the SVG catalog."

[lib]
name = "design_icons"
path = "mod.rs"

[dependencies]
dioxus = { version = "0.7.9", default-features = false, features = ["macro", "html", "signals", "hooks"] }
