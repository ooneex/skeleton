// In TypeScript, `ChartLegend` is `RechartsPrimitive.Legend` — a direct re-export
// of the recharts Legend primitive. Since recharts has no Rust equivalent,
// `ChartLegend` is an alias for `ChartLegendContent` (see `mod.rs`).
// This file exists to mirror the TypeScript source structure.
