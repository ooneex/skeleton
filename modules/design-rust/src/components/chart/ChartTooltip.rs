// In TypeScript, `ChartTooltip` is `RechartsPrimitive.Tooltip` — a direct re-export
// of the recharts Tooltip primitive. Since recharts has no Rust equivalent,
// `ChartTooltip` is an alias for `ChartTooltipContent` (see `mod.rs`).
// This file exists to mirror the TypeScript source structure.
